# =============================================================================
# Sortie — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# All other variables are server-side only (API routes).
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase — https://supabase.com
# Create a project, then find these in: Settings → API
# Used for: authentication, PostgreSQL database, file storage
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# -----------------------------------------------------------------------------
# Anthropic — https://console.anthropic.com
# Used for: business card extraction (Claude Vision) and follow-up email drafting
# Model: claude-sonnet-4-20250514
# Required API permissions: messages:write
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=your-anthropic-api-key

# -----------------------------------------------------------------------------
# OpenAI — https://platform.openai.com
# Used for: voice note transcription (Whisper API)
# Model: whisper-1
# Required API permissions: audio:transcriptions
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your-openai-api-key
